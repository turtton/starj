# AWS S3 (ECS task role + default credentials chain)
storage.s3.region=${STORAGE_S3_REGION}
storage.s3.bucket=${STORAGE_S3_BUCKET}

# ElastiCache Serverless requires TLS in transit
spring.data.redis.ssl.enabled=true
